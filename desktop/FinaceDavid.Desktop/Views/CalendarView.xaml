<UserControl x:Class="FinaceDavid.Desktop.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded"
             Background="Transparent">
    <StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Anterior" Click="Previous_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" />
            <TextBlock Text="{Binding CurrentMonth, StringFormat={}{0:MMMM yyyy}}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,8,0" />
            <Button Content="Próximo" Click="Next_Click" Style="{StaticResource SecondaryButton}" />
        </StackPanel>
        <ItemsControl ItemsSource="{Binding Days}" Margin="0,16,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="7" Rows="6" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="4" Padding="12" CornerRadius="12" Background="{DynamicResource CardBackground}">
                        <StackPanel>
                            <TextBlock Text="{Binding Date, StringFormat=dd}" FontWeight="Bold" HorizontalAlignment="Center" />
                            <TextBlock Text="{Binding TotalIncome, StringFormat=R$ {0:N0}}" Foreground="LightGreen" FontSize="12" HorizontalAlignment="Center" />
                            <TextBlock Text="{Binding TotalOutcome, StringFormat=R$ {0:N0}}" Foreground="Salmon" FontSize="12" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
