<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FinaceDavid.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="clr-namespace:FinaceDavid.ViewModels"
    BackgroundColor="{DynamicResource SurfaceColor}"
    x:DataType="viewmodels:SettingsViewModel">
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="24">
            <Label Text="Tema" FontSize="18" TextColor="{DynamicResource PrimaryTextColor}" />
            <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                <Label Text="Tema escuro" TextColor="{DynamicResource SecondaryTextColor}" VerticalOptions="Center" />
                <Switch IsToggled="{Binding IsDarkTheme}" Toggled="Switch_Toggled" />
            </HorizontalStackLayout>
            <Label Text="Localização" FontSize="18" TextColor="{DynamicResource PrimaryTextColor}" />
            <Label Text="{Binding CurrentLocale}" TextColor="{DynamicResource SecondaryTextColor}" />
            <Label Text="Alterar senha" FontSize="18" TextColor="{DynamicResource PrimaryTextColor}" />
            <Entry Placeholder="Senha atual" Text="{Binding CurrentPassword}" IsPassword="True" MinimumHeightRequest="48" />
            <Entry Placeholder="Nova senha" Text="{Binding NewPassword}" IsPassword="True" MinimumHeightRequest="48" />
            <Button Text="Salvar" Command="{Binding ChangePasswordCommand}" BackgroundColor="{DynamicResource AccentColor}" TextColor="White" CornerRadius="12" HeightRequest="48" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
